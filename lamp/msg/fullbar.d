module msg;

import std.stdio;
import std.stdint;
import std.zlib;

public class SCI_SET {
       public static void say(say, stderr)(ref obj) {
              return SCI_SET.say(say, stderr);
       }
}
