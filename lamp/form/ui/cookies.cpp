//
// Created by admin on 24-2-10.
//

// You may need to build the project (run Qt uic code generator) to get "ui_cookies.h" resolved

#include "cookies.h"


#if 0
cookies::cookies(QWidget *parent) :
    QWidget(parent), ui(new Ui::cookies) {
    ui->setupUi(this);
}

cookies::~cookies() {
    delete ui;
}
#endif // 0